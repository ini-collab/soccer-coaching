<?php
/**
 * サッカーコーチノート チーム共有版 設定ファイル
 *
 * このファイルを「config.php」という名前でコピーして、内容を書き換えてください。
 *   例) ConoHa WINGのファイルマネージャーで config.sample.php を複製 → config.php にリネーム
 */
return [

  /* ----------------------------------------------------------
   * データベース
   * 標準は SQLite（設定不要・data/app.db に自動作成）。
   * ConoHa WINGのMySQLを使う場合は driver を 'mysql' にして接続情報を入力。
   * -------------------------------------------------------- */
  'db' => [
    'driver' => 'sqlite', // 'sqlite' または 'mysql'
    'sqlite_path' => __DIR__ . '/data/app.db',
    'mysql' => [
      'host'    => 'localhost',   // ConoHa WINGでは「DBサーバー名」(例: mysqlXXXX.conoha.ne.jp)
      'dbname'  => '',
      'user'    => '',
      'pass'    => '',
      'charset' => 'utf8mb4',
    ],
  ],

  /* ----------------------------------------------------------
   * 招待コード（新規登録時の合言葉）
   *
   * コーチとプレイヤー（選手・保護者）で別のコードにできます。
   *   coach_invite_code  … コーチ（全機能が使える人）用。★必ず変更してください
   *   player_invite_code … プレイヤー（参照＋自分の出欠のみ）用
   *
   * ・player_invite_code を '' にするとプレイヤーの自己登録を無効にできます
   *   （その場合はコーチが「メンバー」タブで名簿を管理します）
   * ・後方互換：coach_invite_code が未設定なら下の invite_code を使います
   * -------------------------------------------------------- */
  'invite_code'        => 'soccer-team',   // コーチ用（後方互換のため名称は据え置き）
  'coach_invite_code'  => '',              // 空ならinvite_codeを使用
  'player_invite_code' => 'players',       // プレイヤー用（''で自己登録オフ）

  /* ----------------------------------------------------------
   * メール送信（パスワード再設定に使用）
   *   from    … 送信元アドレス（独自ドメインのアドレス推奨）
   *   enabled … false にするとパスワード再設定メールを送りません
   * ConoHa WING は標準の PHP mail() が利用できます。
   * -------------------------------------------------------- */
  'mail' => [
    'enabled'   => true,
    'from'      => 'no-reply@example.com',
    'from_name' => 'サッカーコーチノート',
  ],

  /* ----------------------------------------------------------
   * Googleログイン（任意）
   * Google Cloud Console で OAuth クライアントID（ウェブアプリ）を作成し、
   * 「承認済みのリダイレクトURI」に
   *    https://あなたのドメイン/team/auth_google.php
   * を登録して、client_id / client_secret を貼り付けてください。
   * 空のままにすると「Googleでログイン」ボタンは表示されません。
   * -------------------------------------------------------- */
  'google' => [
    'client_id'     => '',
    'client_secret' => '',
  ],

  /* ----------------------------------------------------------
   * 公開URL（通常は空のままで自動判定されます）
   * リバースプロキシ等で自動判定がうまくいかない場合のみ設定。
   * 例: 'https://example.com/team'
   * -------------------------------------------------------- */
  'base_url' => '',
];
